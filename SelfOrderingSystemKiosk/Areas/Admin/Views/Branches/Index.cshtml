@model IEnumerable<SelfOrderingSystemKiosk.Areas.Admin.Models.Branch>
@{
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
    var branches = Model?.ToList() ?? new List<SelfOrderingSystemKiosk.Areas.Admin.Models.Branch>();
}

<div class="branches-container">
    <div class="page-header">
        <h1>Branch Management</h1>
        <a asp-action="Create" class="btn-primary">
            <i class="bi bi-plus-circle"></i> Add Branch
        </a>
    </div>

    @if (!string.IsNullOrEmpty(ViewBag.Message))
    {
        <div class="alert alert-success">@ViewBag.Message</div>
    }

    @if (!branches.Any())
    {
        <div class="empty-state">
            <i class="bi bi-shop" style="font-size: 3rem; color: #ccc;"></i>
            <h3>No branches yet</h3>
            <p>Get started by adding your first branch location.</p>
            <a asp-action="Create" class="btn-primary">Add Your First Branch</a>
        </div>
    }
    else
    {
        <div class="branches-grid">
            @foreach (var branch in branches)
            {
                <div class="branch-card @(branch.IsActive ? "" : "branch-inactive")">
                    <div class="branch-card-header">
                        <div>
                            <h3>@branch.BranchName</h3>
                            <span class="branch-code">@branch.BranchCode</span>
                        </div>
                        <span class="status-badge @(branch.IsActive ? "status-active" : "status-inactive")">
                            @(branch.IsActive ? "Active" : "Inactive")
                        </span>
                    </div>
                    <div class="branch-card-body">
                        @if (!string.IsNullOrEmpty(branch.Address))
                        {
                            <p class="branch-detail">
                                <i class="bi bi-geo-alt"></i> @branch.Address
                            </p>
                        }
                        @if (!string.IsNullOrEmpty(branch.Phone))
                        {
                            <p class="branch-detail">
                                <i class="bi bi-telephone"></i> @branch.Phone
                            </p>
                        }
                        @if (!string.IsNullOrEmpty(branch.Email))
                        {
                            <p class="branch-detail">
                                <i class="bi bi-envelope"></i> @branch.Email
                            </p>
                        }
                    </div>
                    <div class="branch-card-footer">
                        <a asp-action="Overview" asp-route-branchId="@branch.Id" class="btn-secondary btn-sm">
                            <i class="bi bi-graph-up"></i> Overview
                        </a>
                        <a asp-action="Edit" asp-route-id="@branch.Id" class="btn-secondary btn-sm">
                            <i class="bi bi-pencil"></i> Edit
                        </a>
                        <form asp-action="ToggleStatus" asp-route-id="@branch.Id" method="post" style="display: inline;">
                            @Html.AntiForgeryToken()
                            <button type="submit" class="btn-secondary btn-sm" style="background: @(branch.IsActive ? "#f39c12" : "#27ae60"); border: none; color: white; padding: 6px 12px; border-radius: 6px; cursor: pointer;">
                                <i class="bi bi-toggle-@((branch.IsActive ? "on" : "off"))"></i> @(branch.IsActive ? "Deactivate" : "Activate")
                            </button>
                        </form>
                        <form asp-action="Delete" asp-route-id="@branch.Id" method="post" style="display: inline;">
                            @Html.AntiForgeryToken()
                            <button type="submit" class="btn-danger btn-sm" onclick="return confirm('Are you sure you want to delete branch \'@branch.BranchName\'? This cannot be undone.');">
                                <i class="bi bi-trash"></i> Delete
                            </button>
                        </form>
                    </div>
                </div>
            }
        </div>
    }

    <div class="branch-overview-link" style="margin-top: 24px; padding: 16px; background: linear-gradient(135deg, #667eea 0%, #764ba2 100%); border-radius: 12px; color: white;">
        <div style="display: flex; justify-content: space-between; align-items: center;">
            <div>
                <h3 style="margin: 0 0 8px 0; color: white;"><i class="bi bi-graph-up-arrow"></i> Multi-Branch Overview</h3>
                <p style="margin: 0; color: rgba(255,255,255,0.9);">View consolidated statistics and performance metrics across all branches.</p>
            </div>
            <a asp-action="Overview" class="btn-primary" style="background: white; color: #667eea; text-decoration: none; font-weight: 600;">
                View Overview <i class="bi bi-arrow-right"></i>
            </a>
        </div>
    </div>
</div>

@section Styles {
    <style>
        .branches-container {
            padding: 20px;
        }

        .page-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 24px;
        }

        .page-header h1 {
            margin: 0;
            font-size: 1.8rem;
            color: #2c3e50;
        }

        .btn-primary {
            display: inline-flex;
            align-items: center;
            gap: 8px;
            padding: 10px 20px;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            text-decoration: none;
            border-radius: 8px;
            font-weight: 600;
            border: none;
            cursor: pointer;
            transition: transform 0.2s, box-shadow 0.2s;
        }

        .btn-primary:hover {
            transform: translateY(-2px);
            box-shadow: 0 4px 12px rgba(102, 126, 234, 0.4);
        }

        .btn-secondary {
            display: inline-flex;
            align-items: center;
            gap: 6px;
            padding: 6px 12px;
            background: #f8f9fa;
            color: #495057;
            text-decoration: none;
            border-radius: 6px;
            font-weight: 500;
            border: 1px solid #dee2e6;
            cursor: pointer;
            transition: all 0.2s;
        }

        .btn-secondary:hover {
            background: #e9ecef;
            border-color: #adb5bd;
        }

        .btn-danger {
            display: inline-flex;
            align-items: center;
            gap: 6px;
            padding: 6px 12px;
            background: #fee;
            color: #c0392b;
            text-decoration: none;
            border-radius: 6px;
            font-weight: 500;
            border: 1px solid #f5c6cb;
            cursor: pointer;
            transition: all 0.2s;
        }

        .btn-danger:hover {
            background: #f8d7da;
            border-color: #f1aeb5;
        }

        .btn-sm {
            font-size: 0.85rem;
            padding: 6px 10px;
        }

        .branches-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(320px, 1fr));
            gap: 20px;
            margin-bottom: 24px;
        }

        .branch-card {
            background: white;
            border-radius: 12px;
            box-shadow: 0 2px 8px rgba(0,0,0,0.08);
            overflow: hidden;
            transition: transform 0.2s, box-shadow 0.2s;
            border: 2px solid transparent;
        }

        .branch-card:hover {
            transform: translateY(-4px);
            box-shadow: 0 8px 24px rgba(0,0,0,0.12);
        }

        .branch-card.branch-inactive {
            opacity: 0.75;
            border-color: #e0e0e0;
        }

        .branch-card-header {
            padding: 16px;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            display: flex;
            justify-content: space-between;
            align-items: flex-start;
        }

        .branch-card-header h3 {
            margin: 0 0 4px 0;
            font-size: 1.2rem;
        }

        .branch-code {
            font-size: 0.85rem;
            opacity: 0.9;
            font-family: monospace;
        }

        .status-badge {
            padding: 4px 10px;
            border-radius: 20px;
            font-size: 0.75rem;
            font-weight: 600;
            text-transform: uppercase;
        }

        .status-active {
            background: rgba(39, 174, 96, 0.2);
            color: #27ae60;
        }

        .status-inactive {
            background: rgba(149, 165, 166, 0.2);
            color: #95a5a6;
        }

        .branch-card-body {
            padding: 16px;
        }

        .branch-detail {
            margin: 8px 0;
            color: #555;
            font-size: 0.9rem;
            display: flex;
            align-items: center;
            gap: 8px;
        }

        .branch-detail i {
            color: #667eea;
            width: 18px;
        }

        .branch-card-footer {
            padding: 12px 16px;
            background: #f8f9fa;
            display: flex;
            gap: 8px;
            flex-wrap: wrap;
        }

        .empty-state {
            text-align: center;
            padding: 60px 20px;
            background: white;
            border-radius: 12px;
            box-shadow: 0 2px 8px rgba(0,0,0,0.08);
        }

        .empty-state h3 {
            margin: 16px 0 8px;
            color: #2c3e50;
        }

        .empty-state p {
            color: #7f8c8d;
            margin-bottom: 24px;
        }

        .alert {
            padding: 12px 16px;
            border-radius: 8px;
            margin-bottom: 20px;
        }

        .alert-success {
            background: #d4edda;
            color: #155724;
            border: 1px solid #c3e6cb;
        }
    </style>
}
