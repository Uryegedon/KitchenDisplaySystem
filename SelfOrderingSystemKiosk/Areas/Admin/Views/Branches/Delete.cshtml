@model SelfOrderingSystemKiosk.Areas.Admin.Models.Branch
@{
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}

<div class="branch-delete-container">
    <div class="delete-card">
        <div class="delete-icon">
            <i class="bi bi-exclamation-triangle"></i>
        </div>
        <h1>Deactivate Branch</h1>
        <p class="delete-warning">
            This keeps historical orders, users, inventory, QR tables, and logs linked to this branch.
        </p>

        <div class="branch-info">
            <div class="branch-info-header">
                <h2>@Model.BranchName</h2>
                <span class="branch-code">@Model.BranchCode</span>
            </div>
            @if (!string.IsNullOrEmpty(Model.Address))
            {
                <p class="branch-detail"><i class="bi bi-geo-alt"></i> @Model.Address</p>
            }
            @if (!string.IsNullOrEmpty(Model.Phone))
            {
                <p class="branch-detail"><i class="bi bi-telephone"></i> @Model.Phone</p>
            }
            @if (!string.IsNullOrEmpty(Model.Email))
            {
                <p class="branch-detail"><i class="bi bi-envelope"></i> @Model.Email</p>
            }
        </div>

        <form asp-action="DeleteConfirmed" method="post" class="delete-form">
            @Html.AntiForgeryToken()
            <input type="hidden" asp-for="Id" />
            
            <div class="confirmation-input">
                <label for="confirmName">Type <strong>@Model.BranchName</strong> to confirm:</label>
                <input type="text" id="confirmName" name="confirmName" class="form-control" 
                       placeholder="Type branch name here..." autocomplete="off" />
            </div>

            <div class="form-actions">
                <button type="submit" id="deleteBtn" class="btn-delete" disabled>
                    <i class="bi bi-pause-circle"></i> Yes, Deactivate This Branch
                </button>
                <a asp-action="Index" class="btn-cancel">No, Go Back</a>
            </div>
        </form>
    </div>
</div>

@section Styles {
    <style>
        .branch-delete-container {
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 80vh;
        }

        .delete-card {
            background: #fff;
            padding: 40px;
            border-radius: 12px;
            box-shadow: 0 2px 6px rgba(0, 0, 0, 0.05);
            border: 2px solid #f0d7bf;
            max-width: 500px;
            width: 100%;
            text-align: center;
        }

        .delete-icon {
            width: 80px;
            height: 80px;
            margin: 0 auto 20px;
            background: #fee;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .delete-icon i {
            font-size: 2.5rem;
            color: #e74c3c;
        }

        .delete-card h1 {
            margin: 0 0 12px 0;
            font-size: 1.6rem;
            color: #d35400;
        }

        .delete-warning {
            color: #e74c3c;
            font-weight: 600;
            margin-bottom: 24px;
        }

        .branch-info {
            background: #fffaf5;
            padding: 20px;
            border-radius: 12px;
            margin-bottom: 24px;
            text-align: left;
            border: 2px solid #f0d7bf;
        }

        .branch-info-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 12px;
            padding-bottom: 12px;
            border-bottom: 2px solid #f0d7bf;
        }

        .branch-info-header h2 {
            margin: 0;
            font-size: 1.3rem;
            color: #d35400;
        }

        .branch-code {
            font-family: monospace;
            color: #e67e22;
            font-weight: 600;
        }

        .branch-detail {
            margin: 8px 0;
            color: #555;
            font-size: 0.9rem;
            display: flex;
            align-items: center;
            gap: 8px;
        }

        .branch-detail i {
            color: #e67e22;
            width: 18px;
        }

        .confirmation-input {
            margin-bottom: 20px;
            text-align: left;
        }

        .confirmation-input label {
            display: block;
            margin-bottom: 8px;
            font-weight: 600;
            color: #555;
        }

        .form-control {
            width: 100%;
            padding: 12px 15px;
            border: 2px solid #f0d7bf;
            border-radius: 10px;
            font-size: 1em;
            transition: border-color 0.3s, box-shadow 0.3s;
            box-sizing: border-box;
            background: #fffaf5;
        }

        .form-control:focus {
            outline: none;
            border-color: #e74c3c;
            box-shadow: 0 0 6px rgba(231, 76, 60, 0.3);
        }

        .form-actions {
            display: flex;
            gap: 12px;
            justify-content: center;
        }

        .btn-delete {
            display: inline-flex;
            align-items: center;
            gap: 8px;
            padding: 12px 25px;
            background: #e74c3c;
            color: white;
            border: none;
            border-radius: 10px;
            font-weight: 600;
            font-size: 1em;
            cursor: pointer;
            transition: background-color 0.3s, transform 0.2s;
            opacity: 0.5;
        }

        .btn-delete:not(:disabled) {
            opacity: 1;
        }

        .btn-delete:not(:disabled):hover {
            background: #c0392b;
            transform: translateY(-2px);
        }

        .btn-delete:disabled {
            cursor: not-allowed;
        }

        .btn-cancel {
            display: inline-flex;
            align-items: center;
            padding: 12px 25px;
            background-color: #95a5a6;
            color: white;
            text-decoration: none;
            border-radius: 10px;
            font-weight: 600;
            border: none;
            transition: background-color 0.3s;
        }

        .btn-cancel:hover {
            background-color: #7f8c8d;
        }
    </style>
}

@section Scripts {
    <script>
        const confirmInput = document.getElementById('confirmName');
        const deleteBtn = document.getElementById('deleteBtn');
        const expectedValue = '@Model.BranchName';

        confirmInput.addEventListener('input', function() {
            if (this.value === expectedValue) {
                deleteBtn.disabled = false;
            } else {
                deleteBtn.disabled = true;
            }
        });

        document.querySelector('.delete-form').addEventListener('submit', function(e) {
            if (confirmInput.value !== expectedValue) {
                e.preventDefault();
                alert('Please type the branch name exactly to confirm deactivation.');
            }
        });
    </script>
}
